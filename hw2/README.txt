Memory is the main class, it is in charge of 
-setting up the tasks 
-running the tasks 
-storing the values in a data object
-and then displaying the results

Data is a data structure for each test it 
-stores totalSum, totalTime
-calculates AverageSum and AverageTime 
-plus stores an arrayList of the individual times, that could be output to a csv file
